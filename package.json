{
  "name": "bgwatsapp",
  "version": "1.1.0",
  "private": true,
  "description": "BGWATSAPP full-stack messaging web app",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check:syntax": "node --check server.js && node --check app.js",
    "check:smoke": "node scripts/smoke-test.js",
    "test": "npm run check:syntax && npm run check:smoke"
  },
  "engines": {
    "node": ">=20"
  }
}
